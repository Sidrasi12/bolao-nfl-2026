Bolao.Predictions={
 weeklyDoc(week){return Bolao.db.collection('weeklyPredictions').doc(`${BOLAO_CONFIG.season}_${week}_${Bolao.Auth.user.uid}`)},
 async loadWeekly(week){const d=await this.weeklyDoc(week).get();return d.exists?(d.data().picks||{}):{}},
 async saveWeekly(week,picks){await this.weeklyDoc(week).set({userId:Bolao.Auth.user.uid,userName:Bolao.Auth.user.name,season:BOLAO_CONFIG.season,week,picks,updatedAt:firebase.firestore.FieldValue.serverTimestamp()},{merge:true})},
 async weekly(){const week=+(localStorage.getItem('bolao_week')||1);Bolao.App.content(`<div class="section-title"><h1>Palpites semanais</h1><select id="week" style="width:150px">${Array.from({length:18},(_,i)=>`<option ${i+1===week?'selected':''}>${i+1}</option>`).join('')}</select></div><div id="games" class="card">Carregando jogos...</div>`);document.querySelector('#week').onchange=e=>{localStorage.setItem('bolao_week',e.target.value);this.weekly()};try{this.games=await Bolao.ESPN.games(week);this.picks=await this.loadWeekly(week);this.renderGames(week)}catch(e){document.querySelector('#games').innerHTML=`<div class="notice">Não foi possível carregar: ${e.message}</div>`}},
 renderGames(week){const now=Date.now();document.querySelector('#games').innerHTML=this.games.length?this.games.map(g=>{const locked=now>=new Date(g.date).getTime()-BOLAO_CONFIG.lockMinutes*60000,p=this.picks[g.id]||{};return`<article class="game-card ${locked?'locked':''}"><div class="game"><div class="team"><img src="${g.away.logo}">${g.away.name}</div><div class="game-meta">${new Date(g.date).toLocaleString('pt-BR',{dateStyle:'short',timeStyle:'short'})}<div class="venue">🏟️ ${g.venue}${g.venueCity?' · '+g.venueCity:''}</div><div>${locked?'🔒 Encerrado':'Aberto'}</div></div><div class="team">${g.home.name}<img src="${g.home.logo}"></div></div><div class="choices"><button class="choice ${p.winner===g.away.abbr?'selected':''}" data-g="${g.id}" data-w="${g.away.abbr}">${g.away.abbr}</button><button class="choice ${p.winner===g.home.abbr?'selected':''}" data-g="${g.id}" data-w="${g.home.abbr}">${g.home.abbr}</button><button class="choice ${p.difficulty==='VD'?'selected':''}" data-g="${g.id}" data-d="VD">VD</button><button class="choice ${p.difficulty==='VF'?'selected':''}" data-g="${g.id}" data-d="VF">VF</button></div></article>`}).join(''):'<p>Nenhum jogo encontrado.</p>';document.querySelectorAll('.choice').forEach(b=>b.onclick=async()=>{const id=b.dataset.g;this.picks[id]=this.picks[id]||{};if(b.dataset.w)this.picks[id].winner=b.dataset.w;if(b.dataset.d)this.picks[id].difficulty=b.dataset.d;try{await this.saveWeekly(week,this.picks);this.renderGames(week);Bolao.App.toast('Palpite salvo no Firestore')}catch(e){Bolao.App.toast('Erro ao salvar: '+e.message)}})},
 async preseason(){const ref=Bolao.db.collection('preseasonPredictions').doc(`${BOLAO_CONFIG.season}_${Bolao.Auth.user.uid}`),snap=await ref.get(),p=snap.exists?snap.data().picks||{}:{};const divs=[['AFC','Leste'],['AFC','Oeste'],['AFC','Sul'],['AFC','Norte'],['NFC','Leste'],['NFC','Oeste'],['NFC','Sul'],['NFC','Norte']];const field=(id,label,filter={})=>`<label>${label}<select id="${id}">${Bolao.teamOptions(filter)}</select></label>`;Bolao.App.content(`<div class="section-title"><h1>Pré-temporada</h1><span class="badge">Prazo: 8 de setembro</span></div><form id="pre-form" class="card"><div class="form-grid">${field('champion','Campeão do Super Bowl')}${field('runner','Vice-campeão')}${divs.map(([c,d])=>field(`div_${c}_${d}`,`${c} ${d}`,{conference:c,division:d})).join('')}${field('worst','Pior campanha')}<label>MVP<input id="mvp" placeholder="Nome do jogador"></label></div><h3>Wild Cards</h3><div class="form-grid">${[1,2,3].map(i=>field('wcAFC'+i,'Wildcard AFC '+i,{conference:'AFC'})).join('')}${[1,2,3].map(i=>field('wcNFC'+i,'Wildcard NFC '+i,{conference:'NFC'})).join('')}</div><button>Salvar palpites</button></form>`);Object.entries(p).forEach(([k,v])=>{const e=document.getElementById(k);if(e)e.value=v});document.querySelector('#pre-form').onsubmit=async e=>{e.preventDefault();const picks={};e.target.querySelectorAll('select,input').forEach(x=>picks[x.id]=x.value);try{await ref.set({userId:Bolao.Auth.user.uid,userName:Bolao.Auth.user.name,season:BOLAO_CONFIG.season,picks,updatedAt:firebase.firestore.FieldValue.serverTimestamp()},{merge:true});Bolao.App.toast('Pré-temporada salva no Firestore')}catch(x){Bolao.App.toast('Erro ao salvar: '+x.message)}}}
};