Bolao.Admin={async render(){if(Bolao.Auth.user.role!=='admin')return Bolao.App.navigate('dashboard');Bolao.App.content('<h1>Administração</h1><div class="card" id="users">Carregando participantes...</div>');try{const s=await Bolao.db.collection('users').get();document.querySelector('#users').innerHTML=`<h2>Participantes (${s.size})</h2><div class="table-wrap"><table><thead><tr><th>Nome</th><th>E-mail</th><th>Função</th><th>Ativo</th><th>Pago</th></tr></thead><tbody>${s.docs.map(d=>{const x=d.data();return`<tr><td>${x.name||''}</td><td>${x.email||''}</td><td>${x.role||'player'}</td><td>${x.active!==false?'Sim':'Não'}</td><td>${x.paid?'Sim':'Não'}</td></tr>`}).join('')}</tbody></table></div>`}catch(e){document.querySelector('#users').textContent='Erro: '+e.message}}};