Bolao.Ranking={render(){let me=Bolao.Auth.user.name,rows=[{name:me,pre:0,reg:0,play:0,total:0},{name:'Participante Exemplo',pre:42,reg:115,play:16,total:173},{name:'Outro Participante',pre:35,reg:121,play:12,total:168}].sort((a,b)=>b.total-a.total);Bolao.App.content(`<div class="section-title"><h1>Ranking geral</h1><span class="badge">Atualização automática</span></div><div class="grid"><div class="card"><div class="muted">Participantes</div><div class="metric">${rows.length}</div></div><div class="card"><div class="muted">Arrecadação estimada</div><div class="metric">R$ ${(rows.length*BOLAO_CONFIG.entryFee).toFixed(2)}</div></div><div class="card"><div class="muted">Sua posição</div><div class="metric">${rows.findIndex(x=>x.name===me)+1}º</div></div></div><div class="card table-wrap" style="margin-top:16px"><table><thead><tr><th>#</th><th>Participante</th><th>Pré</th><th>Regular</th><th>Playoffs</th><th>Total</th></tr></thead><tbody>${rows.map((r,i)=>`<tr class="${r.name===me?'me':''}"><td class="rank">${i+1}</td><td>${r.name}</td><td>${r.pre}</td><td>${r.reg}</td><td>${r.play}</td><td><b>${r.total}</b></td></tr>`).join('')}</tbody></table></div><p class="muted">Os dois participantes de exemplo servem apenas para demonstrar o layout. Com Firebase configurado, substitua-os pelos documentos reais.</p>`)}};