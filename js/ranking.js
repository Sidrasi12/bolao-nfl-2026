Bolao.Ranking={async render(){Bolao.App.content('<h1>Ranking geral</h1><div class="card" id="ranking">Carregando participantes...</div>');try{const s=await Bolao.db.collection('users').where('active','==',true).get(),rows=s.docs.map(d=>({uid:d.id,name:d.data().name||d.data().email,total:d.data().totalPoints||0})).sort((a,b)=>b.total-a.total);document.querySelector('#ranking').innerHTML=`<div class="table-wrap"><table><thead><tr><th>#</th><th>Participante</th><th>Pontos</th></tr></thead><tbody>${rows.map((r,i)=>`<tr class="${r.uid===Bolao.Auth.user.uid?'me':''}"><td class="rank">${i+1}</td><td>${r.name}</td><td><b>${r.total}</b></td></tr>`).join('')}</tbody></table></div><p class="muted">A pontuação será atualizada após o processamento dos resultados.</p>`}catch(e){document.querySelector('#ranking').textContent='Erro ao carregar ranking: '+e.message}}};